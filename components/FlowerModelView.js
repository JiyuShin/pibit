/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useState } from 'react';
import { Canvas } from '@react-three/fiber';
import { OrbitControls, useGLTF, Center } from '@react-three/drei';
import styled from 'styled-components';

const ModelContainer = styled.div`
  position: absolute;
  width: 100%;
  height: 100%;
  top: calc(50% + 100px);
  left: 50%;
  transform: translate(-50%, -50%);
  z-index: 1;
`;

function Model({ onModelClick, scale, ...props }) {
  const { scene } = useGLTF('/flower29.glb');
  const [hovered, setHovered] = useState(false);

  useEffect(() => {
    document.body.style.cursor = hovered ? 'pointer' : 'auto';
  }, [hovered]);

  const handleClick = (e) => {
    e.stopPropagation();
    if (onModelClick) {
      onModelClick();
    }
  };

  return (
    <primitive
      object={scene}
      scale={hovered ? scale * 1.05 : scale}
      onPointerOver={(e) => { e.stopPropagation(); setHovered(true); }}
      onPointerOut={(e) => { e.stopPropagation(); setHovered(false); }}
      onClick={handleClick}
      {...props}
    />
  );
}

useGLTF.preload('/flower29.glb');

export default function FlowerModelView({ onModelClick }) {
  return (
    <ModelContainer>
      <Canvas camera={{ position: [0, 12, 7], fov: 45 }}>
        <ambientLight intensity={1.5} />
        <directionalLight position={[10, 10, 5]} intensity={2} />
        <directionalLight position={[-10, -10, -5]} intensity={1} />
        <Center>
          <Model onModelClick={onModelClick} scale={0.07733} />
        </Center>
        <OrbitControls enableZoom={false} autoRotate autoRotateSpeed={4.8} />
      </Canvas>
    </ModelContainer>
  );
} 