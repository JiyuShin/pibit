/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { Suspense, useState, useEffect } from 'react'
import { useRouter } from 'next/router'
import styled from 'styled-components'
import { Canvas } from '@react-three/fiber'
import { useGLTF, OrbitControls, Center } from '@react-three/drei'

const ModelContainer = styled.div`
  position: absolute;
  top: calc(50% + 38px);
  left: calc(50% + 25px);
  transform: translate(-50%, -50%);
  width: 100vw;
  height: 100vh;
  z-index: 999;
  cursor: pointer;
`;

const modelPosition = [0.5, -25.1, 0];

function Model({ scale, router, ...props }) {
  const { scene } = useGLTF('/flower29.glb')
  const [hovered, setHover] = useState(false)

  useEffect(() => {
    document.body.style.cursor = hovered ? 'pointer' : 'auto'
  }, [hovered])

  return (
    <Center
      {...props}
      onPointerOver={() => setHover(true)}
      onPointerOut={() => setHover(false)}
      onClick={() => router.push('/converf')}
    >
      <primitive object={scene} scale={hovered ? scale * 1.05 : scale} />
    </Center>
  );
}

useGLTF.preload('/flower29.glb')

export default function FlowerModelView() {
  const router = useRouter()
  return (
    <ModelContainer>
      <Canvas camera={{ fov: 45, position: [0, 4, 12] }}>
        <Suspense fallback={null}>
          <ambientLight intensity={1.5} />
          <directionalLight intensity={1.5} position={[10, 10, 10]} />
          <Model scale={0.1941699675} position={modelPosition} router={router} />
        </Suspense>
        <OrbitControls
          target={modelPosition}
          enablePan={false}
          enableZoom={false}
          enableRotate={true}
          autoRotate={true}
          autoRotateSpeed={4.8}
        />
      </Canvas>
    </ModelContainer>
  );
} 