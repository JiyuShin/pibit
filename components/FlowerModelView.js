/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { Suspense, useRef } from 'react'
import styled from 'styled-components'
import { Canvas, useFrame } from '@react-three/fiber'
import { useGLTF, OrbitControls } from '@react-three/drei'

const ModelContainer = styled.div`
  position: absolute;
  top: calc(50% + 50px);
  left: calc(50% + 50px);
  transform: translate(-50%, -50%);
  width: 100vw;
  height: 100vh;
  z-index: 999;
`;

const modelPosition = [0.5, -25.1, 0];

function Model(props) {
  const modelRef = useRef()
  const { scene } = useGLTF('/flower29.glb')

  useFrame((state, delta) => {
    if (modelRef.current) {
      modelRef.current.rotation.y += delta * 0.5;
    }
  });

  return <primitive ref={modelRef} object={scene} {...props} />;
}

useGLTF.preload('/flower29.glb')

export default function FlowerModelView() {
  return (
    <ModelContainer>
      <Canvas camera={{ fov: 45, position: [0, 4, 12] }}>
        <Suspense fallback={null}>
          <ambientLight intensity={1.5} />
          <directionalLight intensity={1.5} position={[10, 10, 10]} />
          <Model scale={0.15928627} position={modelPosition} />
        </Suspense>
        <OrbitControls
          target={modelPosition}
          enablePan={false}
          enableZoom={false}
          enableRotate={false}
        />
      </Canvas>
    </ModelContainer>
  );
} 